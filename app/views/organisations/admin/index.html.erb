<h1>Organisations (Admin)</h1>

<%= link_to "New Organisation", new_organisation_path, class: "btn btn-primary" %>

<div class="organisations">
  <% @organisations.each do |org| %>
    <%= render org %>
  <% end %>
</div>